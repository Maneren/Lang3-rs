# tight arithmetic loop - stresses binary ops, locals, comparisons, jumps
fn compute(n)
  let mut sum = 0
  let mut i = 0
  while i < n do
    sum += i * 2 + 1
    i += 1
  end
  return sum
end

let mut total = 0
let mut k = 0
while k < 400 do
  total += compute(20000)
  k += 1
end
println(total)