fn is_even(n)
  if n == 0 then
    return true
  end

  return is_odd(n - 1)
end

fn is_odd(n)
  if n == 0 then
    return false
  end

  return is_even(n - 1)
end

println(is_even(10))
println(is_even(7))
println(is_odd(10))
println(is_odd(7))
